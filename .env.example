# Ejemplo de configuración
# Si queremos volver al localhost no se les olvide vaciar el archivo .env
# Despues lo ejecutamos normalmente sin ejecutar el script de la ip
VITE_API_URL=http://localhost:8080/api/v1